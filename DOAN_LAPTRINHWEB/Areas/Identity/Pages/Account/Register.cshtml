@page
@model RegisterModel

@{
    ViewData["Title"] = "Đăng ký";
}

<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-md-6 col-lg-5">
            <div class="card shadow-lg border-0">
                <div class="card-body p-5">
                    <!-- Header -->
                    <div class="text-center mb-4">
                        <h1 class="h3 mb-3 fw-semibold">Đăng ký tài khoản</h1>
                        <p class="text-muted">Tạo tài khoản để trải nghiệm mua sắm</p>
                    </div>

                    <!-- Register Form -->
                    <form id="registerForm" method="post">
                        <div asp-validation-summary="ModelOnly" class="text-danger text-center small mb-3"></div>

                        <!-- Full Name -->
                        <div class="form-floating mb-3">
                            <input asp-for="Input.FullName" class="form-control" placeholder="Họ và tên" required />
                            <label asp-for="Input.FullName">Họ và tên</label>
                            <span asp-validation-for="Input.FullName" class="text-danger small"></span>
                        </div>

                        <!-- Email -->
                        <div class="form-floating mb-3">
                            <input asp-for="Input.Email" class="form-control" placeholder="name@example.com" required />
                            <label asp-for="Input.Email">Email</label>
                            <span asp-validation-for="Input.Email" class="text-danger small"></span>
                        </div>

                        <!-- Password -->
                        <div class="form-floating mb-3">
                            <input asp-for="Input.Password" class="form-control" placeholder="Mật khẩu" required />
                            <label asp-for="Input.Password">Mật khẩu</label>
                            <span asp-validation-for="Input.Password" class="text-danger small"></span>
                        </div>

                        <!-- Confirm Password -->
                        <div class="form-floating mb-4">
                            <input asp-for="Input.ConfirmPassword" class="form-control" placeholder="Xác nhận mật khẩu" required />
                            <label asp-for="Input.ConfirmPassword">Xác nhận mật khẩu</label>
                            <span asp-validation-for="Input.ConfirmPassword" class="text-danger small"></span>
                        </div>

                        <!-- Register Button -->
                        <button type="submit" class="btn btn-dark w-100 mb-3 py-2">
                            <i class="fas fa-user-plus me-2"></i>Đăng ký
                        </button>

                        <!-- Login Link -->
                        <p class="text-center mb-4">
                            <span class="text-muted small">Đã có tài khoản?</span>
                            <a class="text-decoration-none ms-1 small" asp-page="./Login" asp-route-returnUrl="@Model.ReturnUrl">
                                Đăng nhập ngay
                            </a>
                        </p>

                        <!-- Social Login Divider -->
                        <div class="position-relative my-4">
                            <hr class="text-muted">
                            <p class="small position-absolute top-50 start-50 translate-middle bg-white px-2 text-muted">
                                hoặc đăng ký với
                            </p>
                        </div>

                        <!-- Social Login Buttons -->
                        @if ((Model.ExternalLogins?.Count ?? 0) == 0)
                        {
                            <div class="row g-2">
                                <div class="col">
                                    <button type="button" class="btn btn-outline-secondary w-100" disabled>
                                        <i class="fab fa-google me-2"></i>Google
                                    </button>
                                </div>
                                <div class="col">
                                    <button type="button" class="btn btn-outline-secondary w-100" disabled>
                                        <i class="fab fa-facebook-f me-2"></i>Facebook
                                    </button>
                                </div>
                            </div>
                        }
                        else
                        {
                            <form id="external-account" asp-page="./ExternalLogin"
                                  asp-route-returnUrl="@Model.ReturnUrl" method="post">
                                <div class="row g-2">
                                    @foreach (var provider in Model.ExternalLogins!)
                                    {
                                        <div class="col">
                                            <button type="submit" class="btn btn-outline-secondary w-100"
                                                    name="provider" value="@provider.Name">
                                                @{
                                                    var icon = provider.Name.ToLower() switch
                                                    {
                                                        "google" => "fab fa-google",
                                                        "facebook" => "fab fa-facebook-f",
                                                        _ => "fas fa-external-link-alt"
                                                    };
                                                }
                                                <i class="@icon me-2"></i>@provider.DisplayName
                                            </button>
                                        </div>
                                    }
                                </div>
                            </form>
                        }
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
    .container py-5 {
        padding-top: 80px;
    }

    body {
        padding-top: 80px;
    }

    .card {
        border-radius: 1rem;
    }

    .form-floating > label {
        color: #6c757d;
    }

    .form-control:focus {
        border-color: #212529;
        box-shadow: 0 0 0 0.25rem rgba(33, 37, 41, 0.15);
    }

    .btn-dark {
        font-weight: 500;
    }

        .btn-dark:hover {
            transform: translateY(-1px);
            box-shadow: 0 4px 8px rgba(0,0,0,0.1);
        }

    .btn-outline-secondary {
        font-weight: 500;
    }

        .btn-outline-secondary:hover {
            background-color: #f8f9fa;
            transform: translateY(-1px);
        }
</style>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
